# Workspace

This folder is AnywhereBot's small computer. File tools (`list_dir`, `read_file`, `write_file`, `edit_file`) and `run_shell` are rooted here unless you start the bot with `--allow-host`.

Put notes, drafts, and files the bot should own in this directory. Session logs live next to the repo in `.anywherebot/sessions/`, not here.
